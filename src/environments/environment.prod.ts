/**
 * Este archivo remplaza a environment.ts, fijate en el
 * archivo angular.json que se claro bajo production.
 **/

const api_addr = 'http://80.240.126.155';

export const environment = {
  production: true,
  indexApiUrl: api_addr,
};
